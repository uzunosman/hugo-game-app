---
phase: 01-fix-scoring-multipliers
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - hugo-server/src/models/Game.ts
  - hugo-server/src/models/Player.ts
autonomous: true
requirements:
  - PUAN-01
  - PUAN-02
  - PUAN-03
gap_closure: true

must_haves:
  truths:
    - "Finisher 100+ puanla açılmışsa 1 ek yıldız kazanır (normal bitişe ek olarak)"
    - "Finisher normal bitişte 1 yıldız, joker ile 2, Hugo turunda 2, kimse açmadansa 2 yıldız kazanır — tüm geçerli koşullar çarpılır"
    - "Her yıldız o turun roundTotal'inden 100 puan düşer (hugoMult uygulandıktan sonra, minimum 0)"
    - "RoundResult nesnesi stars: number field'ı içerir"
    - "openingScore (ilk açılış değeri) Player üzerinde takip edilir ve endRound() sonrası sıfırlanır"
  artifacts:
    - path: "hugo-server/src/models/Game.ts"
      provides: "RoundResult interface ile stars field"
      contains: "stars: number"
    - path: "hugo-server/src/models/Game.ts"
      provides: "endRound() içinde yıldız hesabı ve roundTotal formülü güncellendi"
      contains: "preMult - (stars * 100)"
    - path: "hugo-server/src/models/Player.ts"
      provides: "openingScore field (el açılışındaki ilk değer)"
      contains: "openingScore: number"
  key_links:
    - from: "Game.openHand()"
      to: "player.openingScore"
      via: "player.openingScore = totalValue (ilk açılışta set edilir)"
      pattern: "openingScore"
    - from: "Game.endRound()"
      to: "stars calculation"
      via: "player.openingScore >= 100 kontrolü ile yıldız eklenir"
      pattern: "openingScore.*100"
    - from: "endRound() stars calc"
      to: "roundTotal formula"
      via: "roundTotal = preMult - Math.max(0, stars * 100)"
      pattern: "preMult.*stars"
---

<objective>
Yıldız sistemini backend'e ekle: her tur oyuncular yıldız kazanabilir, kazanılan her yıldız o turun roundTotal'inden 100 puan düşer.

Purpose: CONTEXT.md'ye sonradan eklenen yıldız kuralları şu an kodda hiç uygulanmıyor. Bu plan eksik mantığı ekler: Player'a openingScore field'ı, RoundResult'a stars field'ı ve endRound()'a tam yıldız hesabı.

Output: Game.ts (RoundResult.stars + endRound() yıldız mantığı) ve Player.ts (openingScore field).
</objective>

<execution_context>
@/Users/osmanuzun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/osmanuzun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fix-scoring-multipliers/01-CONTEXT.md
@.planning/phases/01-fix-scoring-multipliers/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Mevcut kod — executor okumadan önce bunları bilmeli -->

From hugo-server/src/models/Game.ts (mevcut RoundResult, satır 12-26):
```typescript
export interface RoundResult {
    playerId: string;
    playerName: string;
    isFinisher: boolean;
    handScore: number;
    effectiveHandScore: number;
    penaltyScore: number;
    openBonus: number;
    finishBonus: number;
    rawTotal: number;
    roundTotal: number;          // rawTotal * hugoMult  (GÜNCELLENECEK: preMult - stars*100)
    multiplier: number;
    isHugoRound: boolean;
    finishedWithJoker: boolean;
    // stars: number  ← EKSİK, bu plan ekleyecek
}
```

From hugo-server/src/models/Game.ts (mevcut endRound formülü, satır 825-827):
```typescript
const rawTotal = effectiveHandScore + player.penaltyScore + openBonus + finishBonus;
const roundTotal = rawTotal * hugoMult;        // GÜNCELLENECEK
const multiplier = hugoMult * jokerMult;
```

From hugo-server/src/models/Game.ts (openHand metodu, satır 388-390):
```typescript
player.isOpen = true;
player.lastOpenedValue = totalValue;
player.openedTotal += totalValue;
// player.openingScore = totalValue  ← EKSİK, bu plan ekleyecek
```

From hugo-server/src/models/Player.ts (mevcut alanlar):
```typescript
isOpen: boolean;
lastOpenedValue: number;   // son açılış değeri
openedTotal: number;       // kümülatif açılan değer
penaltyScore: number;
roundScores: number[];
per100PlusCount: number;
// openingScore: number  ← EKSİK, bu plan ekleyecek
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Player.ts'e openingScore field ekle</name>
  <files>hugo-server/src/models/Player.ts</files>

  <read_first>
    - hugo-server/src/models/Player.ts — tam dosya (constructor, resetForNewRound, toPublicJSON)
  </read_first>

  <action>
Player sınıfına `openingScore: number` field'ı ekle. Bu field oyuncunun o turki ilk el açma anındaki toplam değeri tutar (lastOpenedValue ile aynı an set edilir, ama açıkça yıldız hesabı için isimlendirilmiş).

1. Sınıf gövdesinde `per100PlusCount: number;` satırından SONRA ekle:
```typescript
openingScore: number;
```

2. Constructor'da `this.per100PlusCount = 0;` satırından SONRA ekle:
```typescript
this.openingScore = 0;
```

3. `resetForNewRound()` metodunda `this.penaltyScore = 0;` satırından SONRA ekle:
```typescript
this.openingScore = 0;
```

4. `toPublicJSON()` return nesnesinde `per100PlusCount` satırından SONRA ekle:
```typescript
openingScore: this.openingScore,
```

NOT: `openingScore`, `lastOpenedValue` ile farklı — `lastOpenedValue` her yeni açılış işleminde güncellenirken, `openingScore` yalnızca ilk açılışta set edilmeli. Game.ts'teki `openHand()` metodunda bu ayrım Task 2'de yapılacak.
  </action>

  <verify>
    <automated>cd /Users/osmanuzun/projects/hugo-game-app/hugo-server && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - Player.ts sınıf gövdesinde `openingScore: number;` mevcut
    - Player.ts constructor'da `this.openingScore = 0;` mevcut
    - Player.ts `resetForNewRound()` içinde `this.openingScore = 0;` mevcut
    - Player.ts `toPublicJSON()` return nesnesinde `openingScore: this.openingScore,` mevcut
    - `npx tsc --noEmit` 0 ile çıkıyor (TypeScript hatası yok)
  </acceptance_criteria>

  <done>openingScore field Player'a eklendi. endRound() bu değeri yıldız hesabı için okuyabilir.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Game.ts'e openingScore set etme + RoundResult.stars + endRound() yıldız hesabı</name>
  <files>hugo-server/src/models/Game.ts</files>

  <read_first>
    - hugo-server/src/models/Game.ts — tam dosya (özellikle openHand() satır 308-405, endRound() satır 786-872, RoundResult satır 12-26)
    - hugo-server/src/models/Player.ts — Task 1 sonrası güncellenmiş hali (openingScore field'ı confirm et)
    - .planning/phases/01-fix-scoring-multipliers/01-CONTEXT.md — "Yıldız Sistemi" ve "roundTotal Formülü" bölümleri
  </read_first>

  <action>
Üç değişiklik yapılacak:

### Değişiklik 1: RoundResult interface'ine `stars` field ekle

`finishedWithJoker: boolean;` satırından SONRA ekle:
```typescript
stars: number;              // yıldız sayısı (her biri roundTotal'den 100 düşer)
```

### Değişiklik 2: openHand() içinde openingScore set et

`openHand()` metodunda (satır ~388-390 civarı) şu bloku bul:
```typescript
player.isOpen = true;
player.lastOpenedValue = totalValue;
player.openedTotal += totalValue;
```

Hemen arkasına ekle (yalnızca ilk açılışta set edilmeli — `!player.isOpen` kontrolü openHand zaten bunu garantiliyor çünkü isOpen önceden false ise ilk açılış demektir; ancak openHand her çağrıda player.isOpen'ı true set ettiğinden bu mantık doğrudur):
```typescript
// openingScore sadece ilk açılışta set edilir (player.openingScore === 0 kontrolü)
if (player.openingScore === 0) {
    player.openingScore = totalValue;
}
```

### Değişiklik 3: endRound() içinde yıldız hesabı ve roundTotal formülü güncelle

Mevcut endRound() içindeki bu satırları bul (satır ~825-830):
```typescript
const rawTotal = effectiveHandScore + player.penaltyScore + openBonus + finishBonus;
const roundTotal = rawTotal * hugoMult;
const multiplier = hugoMult * jokerMult;

// Apply score to player's cumulative total
player.addScore(roundTotal);
```

Bu satırları şu yeni kodla DEĞİŞTİR:

```typescript
const rawTotal = effectiveHandScore + player.penaltyScore + openBonus + finishBonus;
const preMult = rawTotal * hugoMult;
const multiplier = hugoMult * jokerMult;

// Yıldız hesabı — finisher ve diğer oyuncular için ayrı kural
let stars = 0;
if (isFinisher) {
    // Finisher yıldızı: temel 1, her bonus koşul ×2 yapar
    // finishStars = 1 × (finishedWithJoker?2:1) × (isHugoRound?2:1) × (noOtherPlayersOpened?2:1)
    const jokerStarMult = this.finishedWithJoker ? 2 : 1;
    const hugoStarMult = this.isHugoRound() ? 2 : 1;
    const closedStarMult = noOtherPlayersOpened ? 2 : 1;
    stars = 1 * jokerStarMult * hugoStarMult * closedStarMult;
} else {
    // Diğer oyuncular: 100+ puanla el açtılarsa 1 yıldız
    stars = player.openingScore >= 100 ? 1 : 0;
}

// roundTotal: hugoMult uygulandıktan sonra yıldız düşümü yapılır, minimum 0
const roundTotal = Math.max(0, preMult - (stars * 100));

// Apply score to player's cumulative total
player.addScore(roundTotal);
```

Ayrıca return nesnesine `stars` ekle — mevcut `return { ... }` bloğunda `multiplier` satırından SONRA:
```typescript
stars,
```

Son olarak, mevcut JSDoc yorumunu da güncelle (`roundTotal` satırını değiştir):
```
 *   roundTotal      = Math.max(0, preMult - stars * 100)
 *   preMult         = rawTotal * hugoMult
```

### Yıldız hesabı özeti (CONTEXT.md'den):

| Durum | stars |
|-------|-------|
| Finisher — normal bitiş | 1 |
| Finisher — joker ile | 2 |
| Finisher — Hugo turunda | 2 |
| Finisher — kimse açmadan | 2 |
| Finisher — joker + Hugo | 4 |
| Finisher — joker + kimse açmadan | 4 |
| Finisher — Hugo + kimse açmadan | 4 |
| Finisher — joker + Hugo + kimse açmadan | 8 |
| Diğer oyuncu — 100+ açılış | 1 |
| Diğer oyuncu — 100'den az açılış | 0 |
| Diğer oyuncu — hiç açmamış | 0 |
  </action>

  <verify>
    <automated>cd /Users/osmanuzun/projects/hugo-game-app/hugo-server && npx tsc --noEmit 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - Game.ts `RoundResult` interface'inde `stars: number;` mevcut
    - Game.ts `openHand()` içinde `if (player.openingScore === 0) { player.openingScore = totalValue; }` mevcut
    - Game.ts `endRound()` içinde `const preMult = rawTotal * hugoMult;` mevcut (eski `const roundTotal = rawTotal * hugoMult;` kaldırıldı)
    - Game.ts `endRound()` içinde `const jokerStarMult = this.finishedWithJoker ? 2 : 1;` mevcut
    - Game.ts `endRound()` içinde `const hugoStarMult = this.isHugoRound() ? 2 : 1;` mevcut
    - Game.ts `endRound()` içinde `const closedStarMult = noOtherPlayersOpened ? 2 : 1;` mevcut
    - Game.ts `endRound()` içinde `stars = 1 * jokerStarMult * hugoStarMult * closedStarMult;` mevcut
    - Game.ts `endRound()` içinde `player.openingScore >= 100 ? 1 : 0` mevcut
    - Game.ts `endRound()` içinde `Math.max(0, preMult - (stars * 100))` mevcut
    - Game.ts return nesnesi `stars,` içeriyor
    - `npx tsc --noEmit` 0 ile çıkıyor
  </acceptance_criteria>

  <done>
Yıldız sistemi tam olarak implement edildi:
- RoundResult.stars field'ı eklendi
- Finisher için koşullu yıldız hesabı (1 × joker_mult × hugo_mult × closed_mult)
- Diğer oyuncular için 100+ açılış kontrolü
- roundTotal = Math.max(0, preMult - stars*100) formülü uygulandı
- TypeScript derleniyor hatasız
  </done>
</task>

</tasks>

<verification>
Her iki task tamamlandıktan sonra çalıştır:

```bash
cd /Users/osmanuzun/projects/hugo-game-app/hugo-server && npx tsc --noEmit
```

0 ile çıkmalı, TypeScript hatası olmamalı.

Manuel mantık doğrulama (formül kontrolleri):

- **Normal tur, normal bitiş (finisher):** stars = 1×1×1×1 = 1, roundTotal = 0 - 100 → Math.max(0, -100) = 0
- **Hugo turu, joker bitiş (finisher):** stars = 1×2×2×1 = 4, roundTotal = 0 - 400 → Math.max(0, -400) = 0
- **Hugo turu, joker bitiş, kimse açmadan (finisher):** stars = 1×2×2×2 = 8, roundTotal = 0 - 800 → 0
- **Normal tur, diğer oyuncu 100+ açılış:** stars = 1, preMult = 200 → roundTotal = 200 - 100 = 100
- **Normal tur, diğer oyuncu 80 açılış:** stars = 0, roundTotal = preMult (yıldız yok)
- **Diğer oyuncu hiç açmamış (closedHandPenalty=400):** stars = 0, roundTotal = 400 (yıldız kazanamaz)
</verification>

<success_criteria>
- `hugo-server/src/models/Player.ts` içinde `openingScore: number` field var, constructor'da 0'a initialize edilmiş, `resetForNewRound()` içinde 0'a sıfırlanıyor
- `hugo-server/src/models/Game.ts` içinde `RoundResult` interface'i `stars: number` içeriyor
- `Game.openHand()` içinde `player.openingScore` ilk açılışta set ediliyor
- `Game.endRound()` içinde finisher yıldız formülü: `1 * jokerStarMult * hugoStarMult * closedStarMult`
- `Game.endRound()` içinde non-finisher yıldız kontrolü: `player.openingScore >= 100 ? 1 : 0`
- `Game.endRound()` içinde roundTotal formülü: `Math.max(0, preMult - (stars * 100))`
- Her `RoundResult` nesnesinde `stars` field mevcut
- `npx tsc --noEmit` sıfır ile çıkıyor
</success_criteria>

<output>
After completion, create `.planning/phases/01-fix-scoring-multipliers/01-03-SUMMARY.md`
</output>
